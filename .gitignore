# Compiled binaries
*.exe
/meshmcp
/cmd/meshmcp/meshmcp
cmd/*/mcpserver
cmd/*/mcpecho
cmd/*/mcphttp
cmd/*/bus
cmd/*/vault
cmd/*/scheduler

# NetBird embedded identity / state (per-peer, regenerated on run)
*-nb.json
*-nb-test.json
live-gw.json
router-nb.json
orch-nb.json
livetest-cli.json

# Runtime logs and stores
*.jsonl
/sessions/
/registry-data/

# Secret material — never commit
secrets.json
*-secrets.json
*.secrets.json
audit-signing-key.json
/cosign/

# Live demo runtime state (identities, ledgers, logs)
/demo/*.json
/demo/*.jsonl
/demo/*.log
/demo/*.out
/demo/cosign/

# OS / editor
.DS_Store
*.bak

# OMC operational state (skills are the committable exception)
.omc/
!.omc/skills/

# Prebuilt cmd/ backends — build from source, never commit
/kg
/memory
/bus
/scheduler
/vault

# Stray agent-memory dir accidentally materialized inside the repo — real
# memory lives outside the working tree; never commit this.
/XR_THINKCENTRE/
